config:
  target: 'http://localhost:3030'
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "get_user_test_data.csv"
    fields:
      - "username"
  ensure:
    p95: 200
scenarios:
  - name: "Create and get user"
    flow:
    - get:
        url: "/users/{{ username }}"
    - get:
        url: "/users/{{ username }}"
